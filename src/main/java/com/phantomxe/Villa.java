package com.phantomxe;

import java.math.BigDecimal;

public class Villa extends AbstractHouse {
    public Villa(BigDecimal price, Integer totalArea, Integer numberOfRooms, Integer numberOfSaloons) {
        super(price, totalArea, numberOfRooms, numberOfSaloons);
    }
}
